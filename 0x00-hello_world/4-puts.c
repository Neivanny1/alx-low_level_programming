#include <stdio.h>

/**
 * main - Entry point
 * Return: Always return 0 (Success)
 * This code uses the function puts to print words
 */

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
