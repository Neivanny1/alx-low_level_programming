#include <stdio.h>
/**
 * main - Entry of the program
 * Return: Always returns (0)
 * This programs oupts the string without warning while using -wall gcc
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
